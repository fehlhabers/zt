version: '3'

tasks:
  run:
    cmds:
      - go run ./cmd/zt

  build:
    aliases:
      - b
    cmds:
      - go build -o bin/zt ./cmd/zt

  test:
    aliases:
      - t
    cmds:
      - go test -v ./...
